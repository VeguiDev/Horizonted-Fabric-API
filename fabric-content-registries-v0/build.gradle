version = getSubprojectVersion(project)

// todo: convert access widener to access transformer
//loom {
//	accessWidenerPath = file("src/main/resources/fabric-content-registries-v0.accesswidener")
//}

moduleDependencies(project, [
	'fabric-api-base',
	'fabric-lifecycle-events-v1',
	'fabric-resource-loader-v0'
])

horizon {
	accessTransformerFiles = files(
			"src/main/resources/fabric-content-registries-v0.at"
	)
}
